spring:
  profiles: dev
  datasource:
    url: jdbc:mariadb:http//localhost:3306/hspro?allowMultiQueries=true
    username: devhsh
    password: ehdaofu1!
    driver-class-name: org.mariadb.jdbc.Driver
  web:
    resources:
      cache:
        period: 0  
